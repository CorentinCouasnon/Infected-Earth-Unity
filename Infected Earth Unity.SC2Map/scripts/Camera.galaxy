void Camera_FocusOnUnit(int player, unit u)
{
    CameraPan(player, UnitGetPosition(u), 0, -1, 10, true);
}

void Camera_SetZoomForPlayer(int player, int zoom)
{
    if (zoom < 30 || zoom > 200)
    {
        return;
    }

    CameraSetValue(player, c_cameraValueDistance, zoom, 1, 0, 0);
}